/**
 * 产品名称：quyiyuan.
 * 创建用户：吴伟刚
 * 日期：2015年6月26日11:44:23
 * 创建原因：满意度选择菜单服务
 */
new KyeeModule()
    .group("kyee.quyiyuan.satisfaction.hospitalMenu.service")
    .require([])
    .type("service")
    .name("HospitalMenuService")
    .params([])
    .action(function(){

        var def = {

        };

        return def;
    })
    .build();
